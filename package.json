{
  "name": "pinboard-development-tools",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "duplication": "jscpd --config .jscpd.json --min-lines 4 --min-tokens 40 --reporters ai --no-tips src/pinboard",
    "duplication:ci": "uv run --locked python scripts/check_duplication.py"
  },
  "devDependencies": {
    "jscpd": "5.1.2"
  }
}
