interface:
  display_name: "Slop Cleanup"
  short_description: "Remove code and every orphaned trace"
  default_prompt: "Use $slop-cleanup to remove this code cleanly, collapse everything it leaves behind, and recheck until no new residue remains."
